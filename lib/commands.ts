/**
 * Editor
 */
export const e = (str: string) => `editor.${str}`;

/**
 * Workbench
 */
export const w = (str: string) => `workbench.${str}`;

/**
 * Workbench action
 */
export const wa = (str: string) => `workbench.action.${str}`;

/**
 * Editor action
 */
export const ea = (str: string) => `editor.action.${str}`;
